// @flow

export type CoreConfig = {
  projectId: string,
  fixturesDir: string,
  fixtureFileSuffix: string
};

export type RendererCoordinatorConfig = {
  webUrl: null | string,
  enableRemote: boolean
};

export type PlaygroundConfig = {
  core: CoreConfig,
  rendererCoordinator: RendererCoordinatorConfig
};
