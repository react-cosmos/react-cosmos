import styled from 'styled-components';

export const IllustrationContainer = styled.div`
  --size: 320px;
  display: flex;
  width: var(--size);
  height: var(--size);
  padding: 0 32px;
`;

export const Unbreakable = styled.span`
  white-space: nowrap;
`;
