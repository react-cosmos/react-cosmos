import styled from 'styled-components';

// TODO: Add padding (to look good when resizing window)
export const IllustrationContainer = styled.div`
  --size: 320px;
  display: flex;
  width: var(--size);
  height: var(--size);
`;
