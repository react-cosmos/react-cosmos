language: node_js
sudo: required
node_js:
  - 10
  - 8
  # - 6
before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash
  - export PATH=$HOME/.yarn/bin:$PATH
install: 'yarn install --ignore-engines --frozen-lockfile'
script:
  - yarn test:ci
  # Test source (with cross-package coverage)
  # - yarn src
  # - yarn type-check
  # - yarn lint
  # - yarn test:coverage --maxWorkers=2
  # - yarn codecov
  # # Test build
  # - yarn dist
  # - yarn build
  # - yarn export
cache:
  yarn: false
  directories:
    - ~/.cache
