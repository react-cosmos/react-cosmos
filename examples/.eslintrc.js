module.exports = {
  rules: {
    // Packages are installed separately per example and this rule throws if npm
    // install isn't ran in all examples before running lint command
    "import/no-unresolved": 0
  }
};
